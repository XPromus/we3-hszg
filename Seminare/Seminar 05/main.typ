= Seminaraufgaben 12.11.2025

#set heading(numbering: "1")
== Relationen
Jedes Todo Item soll einem Nutzer gehören.\ 

Die Todo Items müssen mit einem Verweis auf ihren User erweitert werden.
Erschaffe eine Many to One Relation zwischen den Usern und Todo Items.
Überprüfe die Funktionalität über die Swagger UI vom Backend.

== Docker für eine PostgreSQL Datenbank
- Schreibe eine `compose.yml` Datei zum hosten einer PostgreSQL Datenbank
- Start den Container mit Docker oder Podman
- Überprüfe die Funktionalität der Datenbank in IntelliJ

== Konfiguration der Spring Anwendung für PostgreSQL
- Schreibe eine `application.yml` um die Spring Anwendung mit der PostgreSQL zu verbinden
- Importiere die Dependency für PostgreSQL
- Überprüfe die Funktionalität
